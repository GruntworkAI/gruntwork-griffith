---
name: gh-cli-agent
description: Uses the GitHub CLI.
---

This agent uses `gh` — but agents/*.md is not in the info rule's context, so no finding here.
The `gh ` info rule is scoped to hooks/**/* only.
